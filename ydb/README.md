# YDB / YQL scripts

Place here all YQL scripts used for Task 1.

Recommended files:

- `create_transactions_v2.yql` — schema for `transactions_v2`.
- `check_transactions_v2.yql` — quick validation queries after loading data.
- screenshots in `../docs/images/` showing YDB table and DataTransfer result.
